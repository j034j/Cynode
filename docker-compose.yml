services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: cynode
    ports:
      - "5432:5432"
    volumes:
      - cynode_pgdata:/var/lib/postgresql/data

volumes:
  cynode_pgdata:

